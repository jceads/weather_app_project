import 'package:flutter/material.dart';

class stdDivider extends Divider {
  const stdDivider({Key? key})
      : super(
          key: key,
          color: Colors.white12,
          thickness: 2,
          height: 10,
        );
}
